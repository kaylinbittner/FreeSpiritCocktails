<div class="container">
  <div class="row">
    <% @cocktails.each do |c| %>
    <div class="col-xs-12 col-sm-4">
      <%= link_to cocktail_path(c) do %>
      <div class="card">
        <div class="card-category"> <%= c.name %> </div>
        </div>
        <% end %>
      </div>
       <% end %>
    </div>
  </div>
</div>
