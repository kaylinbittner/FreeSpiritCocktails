<%= simple_form_for [cocktail, dose], :html => { class: 'form' } do |f| %>
  <%= f.input :description %>
  <%= f.association :ingredient %>
  <%= f.submit "Add Ingredients", class: "btn btn-primary center-block" %>
<% end %>
